import { connect } from 'react-redux';
import { CPintro } from '../components';

const mapStateToProps = (state) => ({

});

const mapDispatchToProps = (dispatch) => ({

});

const CTintro = connect(
    mapStateToProps,
    mapDispatchToProps
)(CPintro);

export default CTintro;